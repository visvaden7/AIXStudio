import {FunctionComponent} from "react";

export const RegisterPage: FunctionComponent = () => {
  return (
    <div>
      <form>
        <input type={'text'} />
        <input type={'password'} />
        <button>가입하기</button>
      </form>
    </div>
  )
}